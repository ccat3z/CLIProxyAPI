services:
  cli-proxy-api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    command: -port 8098
    network_mode: host
    volumes:
      - ./data:/data
    restart: unless-stopped
