# Pick Upstream Todo

Base commit: `a5cb8832`
Upstream HEAD: `4c0c6029`
Total: 109 commits

| # | SHA | Message | Status |
|---|-----|---------|--------|
| 1 | f5484b09 | fix(registry): Conform Claude models listing to Anthropic API schema | ⚠️ conflicts resolved |
| 2 | e3301ecc | fix(registry): Emit Claude model created_at as RFC 3339 string | ✅ clean |
| 3 | c354f88f | feat(api): Route Anthropic /v1/models requests to the Claude format | ⚠️ conflicts resolved |
| 4 | 1ed1f7b3 | Merge branch 'dev' into main | ✅ empty (merge of removed features) |
| 5 | 13f51d96 | fix(pluginhost): avoid holding host lock during plugin lifecycle | ❌ skipped (plugin code removed on custom branch) |
| 6 | a65ced4a | fix(management): reload plugins asynchronously after changes | ❌ skipped (code removed on custom branch) |
| 7 | 7f026e1a | Add runtime config clone | ✅ clean |
| 8 | a4756ab7 | Use config snapshots for management reload | ⚠️ conflicts resolved |
| 9 | 7b16321e | Stabilize management reload race tests | ⚠️ conflicts resolved |
| 10 | a3c87cee | Fix management reload snapshot ordering | ⚠️ conflicts resolved |
| 11 | 09596d2f | Treat loading plugins as busy | ❌ skipped (code removed on custom branch) |
| 12 | 125c0928 | Merge pull request #3872 from router-for-me/codex/pluginhost-async-reload | ✅ empty commit (code removed/already present on custom) |
| 13 | 8d2c00c1 | feat(plugin-config): update default plugin `Enabled` behavior to false and expand test coverage | ❌ skipped (code removed on custom branch) |
| 14 | b9d024af | feat(executor): handle usage limit errors and enhance retry logic | ❌ skipped (code removed on custom branch) |
| 15 | 8c6f279f | refactor(tests): remove obsolete test files and update reasoning effort logic | ⚠️ partial (applied validate.go isOpenAIFamily xai removal; conflict on test/thinking_conversion_test.go resolved by keeping custom deletion — e2e file imports removed providers; preserved apply_user_defined_test.go + reasoning_effort_test.go as sole coverage since e2e replacement doesn't exist on custom) |
| 16 | 0e81dee7 | Merge pull request #3873 from router-for-me/thinking | ✅ empty commit (duplicate of #15 partial; validate.go xai removal already applied, kimi/xai test files already removed, e2e test kept deleted — imports removed providers; old unit tests preserved as sole coverage) |
| 17 | c2967908 | feat(misc): align Antigravity runtime UA with agy CLI version sources | ❌ skipped (Antigravity code removed on custom branch) |
| 18 | 29f22acd | Merge pull request #3877 from sususu98/feat/antigravity-cli-ua-upstream-dev | ✅ empty commit (Antigravity code removed on custom branch) |
| 19 | 96a8b0cf | feat(executor): normalize reasoning text events and enhance handling logic | ❌ skipped (code removed on custom branch) |
| 20 | 644ba74b | feat(videos): implement auth binding for video requests and enhance proxy handling | ❌ skipped (VIDEOS code removed on custom branch) |
| 21 | f23fb122 | feat(translator): ensure tool uses stay adjacent to tool results in message generation | ✅ |
| 22 | acaf250f | feat(management): add test to validate priority preservation in auth file uploads | ❌ skipped (auth-file upload subsystem removed on custom branch: UploadAuthFile handler, buildAuthFromFileData, cfg.AuthDir field, and codex/antigravity/gemini/kimi/xai auth packages all removed) |
| 23 | cde5081e | test(translator): add tests to validate omission of top-level `output_text` in OpenAI responses | ✅ picked (clean; openai-responses translator survived; all 3 new tests pass) |
| 24 | dd49a520 | feat(translator): add tests to validate trailing assistant prefill stripping and sanitize tool call IDs | ✅ picked (conflicts on Gemini translator files — removed on custom; Claude translator changes integrated cleanly: util.SanitizeClaudeToolID added to chat-completions + responses request converters; 2 new Claude tests pass) |
| 25 | 78ba8ba7 | chore: remove Gemini CLI-related translator packages and logic | ✅ picked (most touched files already removed on custom; integrated surviving gemini-cli cleanups: removed dead `GeminiCLI` constant, `FormatGeminiCLI`, `GeminiCLIUserAgent`, GCP project types, `ProjectSelectionError`, gemini virtual-parent scheduler logic, gemini usage parsers, gemini-cli refs in thinking/strip+validate, file.go gemini→skip + test; dropped dead `json_retry_helpers.go`/`login_prompt.go` whose consumers are gone) |
| 26 | 365e8fc2 | feat(antigravity): HOME reasoning replay for Gemini models | ✅ picked (antigravity executor + reasoning-replay cache/signature_cache all removed on custom; integrated only surviving shared change: `.gitignore` adds `/auths`, `.claude/*`, `.claude` tooling-metadata entries) |
| 27 | 62c4b377 | Revert "feat(antigravity): HOME reasoning replay for Gemini models" | ✅ picked (revert of #26; antigravity files already absent on custom so deletions are no-ops/modify-delete resolved by keeping removed; only surviving change is reverting the `.gitignore` entries #26 added — removed `/auths` and bare `.claude`, keeping `.claude/*`/`auths/*`) |
| 28 | 292456a8 | feat(antigravity): HOME reasoning replay for Gemini models | ✅ picked (antigravity executor + reasoning-replay cache/signature_cache all removed on custom; integrated only surviving incidental change: `.gitignore` adds `/auths` and bare `.claude` entries) |
| 29 | b17d29ad | fix(antigravity): insert replayed functionCall before matching functionResponse | ❌ skipped (antigravity code removed on custom branch) |
| 30 | ef19f5fc | fix(antigravity): address review on replay call_id and args parsing | ❌ skipped (antigravity code removed on custom branch) |
| 31 | c55157dc | fix(antigravity): PR review replay scope, signature merge, and tool keys | ❌ skipped (antigravity code removed on custom branch) |
| 32 | ec8c2c29 | test(antigravity): cover invalid-signature replay cache clear | ❌ skipped (antigravity code removed on custom branch) |
| 33 | ac8fb970 | feat(thinking): remove `thinkingConfig` for `ModeNone` with zero budget and no level | ❌ skipped (touches only `internal/thinking/provider/{antigravity,gemini,geminicli}/apply.go` and `test/thinking_conversion_test.go`, all removed on custom; no surviving core thinking change to integrate) |
| 34 | c13dbcc2 | feat(translator): add test and logic to ensure `object` schemas include `properties` field | ✅ picked |
| 35 | 41c52b9d | test(management): add concurrency test for Codex OAuth session handling | ❌ skipped (codex/oauth code removed on custom branch) |
| 36 | ae6c5eae | feat(runtime): add support for `gpt-image-1.5` and direct image API proxying | ⚠️ partial (codex executor + codex_openai_images*.go removed on custom; kept surviving handler changes: `isOpenAIImageOnlyModel` helper covering gpt-image-1.5/2 in handlers.go, `gptImage15Model` const + `isDefaultImagesToolModel`→`isCodexImagesToolModel` rename in openai_images_handlers.go, `gpt-image-1.5` added to visibility override in codex_client_models.go, test updates; dropped hunks for codex_openai_images.go/test, model_definitions.go builtins, sdk_config.go GPTImage2BaseModel field, config.example.yaml comment) |
| 37 | 052f1934 | fix(auth): classify transport errors as `home_unavailable` with retryable flag | ❌ skipped (change is entirely within `pickNextViaHome`'s `RPopAuth` error path, which is inert on custom: `HomeEnabled()` is hardcoded `false` and `currentHomeDispatcher` always returns `nil`, so the path is unreachable; the `home.ErrAuthNotFound` sentinel belongs to the removed `internal/home` package and the test file `home_websocket_reuse_test.go` is deleted on custom — not shared Claude/OpenAI-compat auth classification) |
| 38 | 1d0551a9 | feat(config): improve config reload handling and introduce async management save hook | ✅ integrated (config-reload/management-save-hook survive on custom; dropped plugin-subsystem hunks: `WithPluginHost` line in builder.go, `syncPluginRuntime`→kept custom `reregisterExecutors`, `setPluginAuthParser`/`SetPluginAuthParser`/`PluginAuthParser` field+method+factory wiring, and the `if !homeEnabled` wrapper around the watcher reloadCallback; kept `ReloadConfigIfChanged`, `applyWatcherConfigUpdate`/`applyConfigUpdateWithAuthSynthesis` split, `reloadConfigFromWatcher`, and the `registerConfigAPIKeyAuths` call after `coreManager.Load`; handler.go persistLocked async-save hook + config_reload.go `ReloadConfigIfChanged` exported wrapper applied cleanly) |
| 39 | c020e2d0 | feat(translator): drop `apply_patch` custom tool in OpenAI responses | ✅ integrated (cherry-pick clean; both files under `internal/translator/claude/openai/responses/` survive on custom — `convertResponsesToolToClaudeTools` `default` branch now skips `apply_patch` custom tools via new `isOpenAIResponsesApplyPatchCustomTool` helper, plus the `TestConvertOpenAIResponsesRequestToClaude_DropsApplyPatchCustomTool` test; no removed-subsystem hunks) |
| 40 | 893412e9 | feat(translator): normalize `service_tier` in Codex requests and add tests | ❌ skipped (touches only `internal/translator/codex/claude/codex_claude_request.go` + test — Codex translator removed on custom; `normalizeCodexServiceTier`/`ConvertClaudeRequestToCodex` do not exist; surviving openai/claude translators handle `service_tier` independently via simple pass-through and are NOT modified by this commit) |
| 41 | 4926630a | feat(translator): support namespace tools in OpenAI response transformations | ✅ picked (clean; all 5 files under `internal/translator/openai/openai/responses/` survive on custom — `convertResponsesToolToOpenAIChatTools` flattens namespace tools on request, `applyResponsesFunctionCallNamespaceFields`/`pickRequestJSON`/`splitResponsesQualifiedFunctionCallFromRequest` restore name+namespace on stream+non-stream responses, new `openai_openai-responses_tools.go` added; 3 new tests pass; no removed-subsystem hunks) |
| 42 | d33ac5e1 | feat(auth): add transient error cooldown configuration and adjust retry logic | ✅ integrated (shared auth/conductor survives on custom; `SetTransientErrorCooldownSeconds`/`nextTransientErrorRetryAfter` in conductor.go, `TransientErrorCooldownSeconds` config field in config.go/parse.go defaults, config_diff.go change-detection, `config.example.yaml` doc, main.go/server.go/service.go wiring, 3 new conductor_overrides_test.go tests + config_diff_test.go assertions all applied; conflicts in server.go and config_diff_test.go resolved: dropped `applySignatureCacheConfig` line — function removed on custom via antigravity/signature removal — and dropped reintroduced `AuthDir`/`CodexKey`/`GeminiKey`/`QuotaExceeded`(Antigravity) struct fields from test fixtures that were removed on custom; redisqueue line in main.go auto-merged to custom's existing `coreauth.SetQuotaCooldownDisabled` context) |
| 43 | 07c297a5 | feat(auth): add persistent cooldown state management with file-backed store | ✅ integrated (shared auth/conductor survives on custom; new `cooldown_state.go`/`cooldown_state_test.go` applied clean; conductor.go `cooldownStore` field, `SetCooldownStateStore`, `RestoreCooldownStates`, `persistCooldownStates`, `clearDisabledCooldownStates`, `cooldownDisabledForAuth`, MarkResult state-change tracking, `applyAuthFailureState(...,disableCooling)` signature, Register/Update/Remove persist hooks all applied; config `SaveCooldownStatus` field + parse.go/config_diff.go/config.example.yaml + config_diff_test.go assertions applied; conflicts resolved dropping removed-subsystem hunks: Manager struct `pluginScheduler` (plugins removed), SetConfig `cfg.Home.Enabled`/`clearHomeRuntimeAuths` (home removed), service.go `applyPprofConfig`/`syncPluginRuntime`/`forceHomeRuntimeConfig`/`applyHomeOverlay`/`startHomeSubscriber`/`startHomeUsageForwarder` + `cfg.Home.Enabled` guards (home/pprof/plugin removed), `resolveCooldownStateAuthDir`→`resolveCooldownStateDir` rewritten to use `filepath.Dir(configPath)` since `cfg.AuthDir`/`util.ResolveAuthDir` removed via auth-dir removal, dropped home-related `service_stale_state_test.go` tests for removed `forceHomeRuntimeConfig`/`applyHomeOverlay`; net-zero diff on that test file) |
| 44 | aed54adb | feat(translator): preserve structured `tool_choice` in OpenAI response conversions | ✅ picked (clean; both files under `internal/translator/openai/openai/responses/` survive on custom — `ConvertOpenAIResponsesRequestToOpenAIChatCompletions` switched from `sjson.SetBytes(...,toolChoice.String())` to `sjson.SetRawBytes(...,[]byte(toolChoice.Raw))` so a structured `{type, function}` tool_choice is preserved as an object instead of being stringified; new `TestConvertOpenAIResponsesRequestToOpenAIChatCompletions_PreservesStructuredToolChoice` test passes; no removed-subsystem hunks) |
| 45 | aa2ad995 | feat(translator): preserve `input_image` details in OpenAI response conversion | ✅ picked (clean; both files under `internal/translator/openai/openai/responses/` survive on custom — in `ConvertOpenAIResponsesRequestToOpenAIChatCompletions`, when an `input_image` content item carries a `detail` field, it is now copied into the emitted `image_url.detail` of the chat-completions content part; new `TestConvertOpenAIResponsesRequestToOpenAIChatCompletions_PreservesInputImageDetail` test passes; no removed-subsystem hunks) |
| 46 | 041a065b | Merge branch 'remove-gemini-cli' into dev | ✅ empty pick (gemini-cli removal already integrated on custom via #25 78ba8ba7; cherry-pick `-m 1` produced only modify/delete conflicts on files already removed and content conflicts where custom had removed additional subsystems the merge left unchanged from base — all resolved by keeping deletions/HEAD; new files `internal/cmd/login_prompt.go` and `internal/runtime/executor/helps/json_retry_helpers.go` had zero callers on custom since `login.go`/`gemini_executor.go` are removed, so they were `git rm`'d as dead code; net diff empty, committed as empty commit to advance base pointer) |
| 47 | 1b849b6d | feat(translator): attach `reasoning_content` to assistant and tool messages in OpenAI response conversion | ✅ clean |
| 48 | 15817006 | chore(deps): bump `github.com/jackc/pgx/v5` from v5.7.6 to v5.9.2 | ❌ skipped (only touches go.mod/go.sum for the `github.com/jackc/pgx/v5` line — Postgres storage backend was removed on custom via `docs/custom/removed-storage-relay.md`, so the pgx dependency and its transitive deps (pgpassfile, pgservicefile, puddle) no longer exist in go.mod/go.sum; cherry-pick `--no-commit` produced content conflicts where HEAD has no pgx line at all and upstream tries to re-add it — applying would resurrect a removed dependency; verified `grep -rn "jackc/pgx\|pgx/v5" --include="*.go"` is empty and go.mod/go.sum have zero pgx/jackc references; not a no-op to integrate, it's a regression; base commit NOT advanced) |
| 49 | 51aa5ba9 | feat(translator): preserve `input_audio` fields in OpenAI request conversions | ❌ skipped (only touches `internal/translator/{codex,gemini}/openai/chat-completions/{codex,gemini}_openai_request{,_test}.go` — both the Codex and Gemini translators were removed on custom via `docs/custom/removed-providers-oauth.md`; cherry-pick `--no-commit` produced only modify/delete conflicts with every path "deleted by us"; verified via `grep -rn "ConvertOpenAIRequestToGemini\|ConvertOpenAIRequestToCodex\|input_audio\|openAIInputAudioMimeType" --include="*.go"` that the only hits were the cherry-picked files left in tree — no surviving equivalent in the claude/openai-compat/common translators to port the `input_audio` preservation logic to; base commit NOT advanced) |
| 50 | 34639c3c | feat(translator): defer Codex function call starts until function name is available | ❌ skipped (only touches `internal/translator/codex/claude/codex_claude_response{,_test}.go` — the Codex translator was removed on custom via `docs/custom/removed-providers-oauth.md`; verified `git ls-tree -r HEAD --name-only | grep -i codex` shows only model-catalog files (`internal/registry/codex_client_models.go`, `internal/registry/models/codex_client_models.json`, `sdk/api/handlers/openai/codex_client_models.go`), none of which are touched by this commit, and `git cat-file -e HEAD:internal/translator/codex/claude/codex_claude_response.go` is ABSENT; no surviving shared openai/claude/common translator code is modified, so there is nothing to port; base commit NOT advanced) |
| 51 | c44d4fcc | feat(schema): add removal of `$comment` and `enumDescriptions` in JSON schema processing | ✅ integrated (schema processing package survives on custom; `internal/util/gemini_schema.go` one-line change applied — added `"$comment", "enumDescriptions"` to the unsupported-keywords list in `removeUnsupportedKeywords`, and `internal/util/gemini_schema_test.go` test expansions applied adding the fields at root/nested + as property names in both input and expected output; dropped the antigravity executor test hunk since `internal/runtime/executor/antigravity/` was removed on custom via `removed-providers-oauth.md` — that conflict was resolved by `git rm`; schema `go test` ran fresh 0.007s) |
| 52 | 4c78e40d | feat(auth): unify provider key handling with OpenAI compatibility support | ⚠️ partial (shared auth/key handling for Claude + OpenAI-compat survives on custom; integrated surviving changes: added `OpenAICompatibleProviderKey` helper in `internal/util/provider.go`, `synthesizer/config.go` now emits namespaced `internalProviderKey` (`openai-compatible-<name>`) into `Provider`/`provider_key` attrs, `scheduler.go` `upsertAuthLocked`/`buildScheduledAuthMeta` switched to `executorKeyFromAuth`, `conductor.go` auto-merged `openAICompatProviderKey`/`closestCooldownWait`/`retryAllowed`/`pickNextLegacy`/`pickNext`/`pickNextMixedLegacy`/`pickNextMixed`/`homeRuntimeAuthByID`/`pickNextViaHome`/`executorKeyFromAuth` to use `util.OpenAICompatibleProviderKey`/`executorKeyFromAuth`, `service.go` `openAICompatInfoFromAuth` switched to namespaced keys + added `internal/util` import, `openai_compat_pool_test.go` updated to `openAICompatPoolProviderKey="openai-compatible-pool"` const, `config_test.go` kept new `TestConfigSynthesizer_OpenAICompat_UsesNamespacedProviderKey` and updated `TestConfigSynthesizer_AllProviders` expected list to `["claude","openai-compatible-compat"]`; dropped removed-subsystem hunks: conductor.go antigravity-credits block `findAllAntigravityCreditsCandidateAuths`/`creditsCandidateEntry`/`hasAntigravityProvider`/`shouldAttemptAntigravityCreditsFallback`/`tryAntigravityCreditsExecute`/`tryAntigravityCreditsExecuteStream`/`antigravityCreditsKVUnavailableError` (antigravity removed), config_test.go `TestConfigSynthesizer_VertexCompat`+`_SkipsEmptyAndHeaders` (vertex/`config.VertexCompatKey` removed), and the entire `sdk/cliproxy/service_executor_registration_test.go` (modify/delete — was deleted on custom; both new tests depend on `internal/pluginhost`+`registerPluginExecutors`+`pluginHost` and `runtimeexecutor.KimiExecutor`, all removed on custom; OpenAICompat namespacing coverage retained via config_test.go + openai_compat_pool_test.go); `gofmt`+`go build`+`go test ./...`+`pytest integration/ -v`(37 passed)+smoke test all pass) |
| 53 | 75fa6265 | feat(executor): normalize `parallel_tool_calls` based on `tools` presence | ❌ skipped (only touches `internal/runtime/executor/codex_executor.go` + new `internal/runtime/executor/codex_executor_parallel_tool_calls_test.go` — both removed on custom via `docs/custom/removed-providers-oauth.md` (the Codex executor and all codex files were deleted); only Claude and OpenAI-compat executors survive on custom, and neither contains an equivalent Codex `parallel_tool_calls`/`tools` normalization path to port the logic to; verified `git cat-file -e HEAD:internal/runtime/executor/codex_executor.go` is ABSENT and `grep -rn "normalizeCodexParallelToolCallsForTools\|parallel_tool_calls" internal/runtime/executor/ --include="*.go"` is empty; base commit NOT advanced) |
| 54 | bc652c7b | feat(translator): add support for `text.format` conversion in OpenAI to Gemini requests | ❌ skipped (Gemini translator code removed on custom branch) |
| 55 | 28e2f979 | feat(executor): add session isolation for `grok-composer` models | ❌ skipped (xai/grok executor code removed on custom branch) |
| 56 | 379167c9 | feat(translator): add benchmarking for `convertSystemRoleToDeveloper` with large inputs | ❌ skipped (codex translator removed on custom branch: `internal/translator/codex/` deleted; both touched files gone, `convertSystemRoleToDeveloper` has zero surviving callers) |
| 57 | f66376f0 | feat(auth): add per-auth OAuth model alias support | ❌ skipped (every hunk depends on infrastructure removed on custom; the entire OAuth model alias feature is gone — `git grep -l "OAuthModelAlias" HEAD -- '*.go'` is empty: `config.OAuthModelAlias` type + `SanitizeOAuthModelAlias` (removed from config), `Manager.SetOAuthModelAlias`/`oauthModelAlias` field, `OAuthModelAliasChannel`, and `applyOAuthModelAlias` in service.go are all absent; `sdk/cliproxy/auth/oauth_model_alias.go` was refactored away into `model_alias.go` whose surviving content is the generic API-key-based alias helpers, not OAuth aliases; the `file.go` hunks call `coreauth.SetOAuthModelAliasesAttribute` (new, depends on removed logic) and live partly inside the `ctx.PluginAuthParser` block that was removed with the plugin system; the `service.go` hunks touch `tryRegisterPluginModelsForAuth`/`applyOAuthModelAliasForAuth` (plugin host + removed alias infra); `config.example.yaml` documents the removed `oauth-model-alias` block and `vertex-api-key`; custom uses per-API-key model aliases via `ClaudeKey.Models[].Alias` instead — a separate mechanism untouched by this commit; base commit NOT advanced) |
| 58 | 790ec307 | feat(config): add support for `rebuild_mid_system_message` configuration | ✅ picked (config + Claude executor + management handler + watcher + tests integrated; dropped the re-added `isClaudeOAuthToken`/OAuth tool-name helpers and `proxy-url` struct field that don't exist on custom; added a local `contextWithGinHeaders` test helper since custom removed the shared one; conflicts in config.example.yaml, config_lists.go, claude_executor.go resolved preserving custom logic; base commit advanced to 790ec307) |
| 59 | b0ca3794 | Merge pull request #3834 from dcrdev/main | ✅ empty (merge of #1-#4: f5484b09/e3301ecc/c354f88f/1ed1f7b3 all already integrated; cherry-pick `-m 1` produced content conflicts on home-subsystem code in server.go — `handleHomeModels`/`handleHomeCodexClientModels`/`geminiModelsHandler`/`loadHomeModelEntries` + `s.cfg.Home.Enabled` guard — removed on custom via removed-redis-home.md, kept HEAD; model_registry.go conflict preserved custom `model.Extra` field; server_test.go auto-merge created a duplicate `TestModelsDispatchByAnthropicVersionHeader` (already added via #3) and an orphaned `TestAmpProviderModelRoutes` testing non-existent `/api/provider/*` routes — both dropped; net diff empty, committed as empty commit to advance base pointer) |
| 60 | a79ae80f | feat(registry): improve model fallback logic and refactor Claude model handling | ✅ picked (model_registry.go: exported `DefaultClaudeMaxInputTokens`/`DefaultClaudeMaxOutputTokens`, `convertModelToMap` claude case now always emits `type:"model"` and uses exported defaults; cache test updated to exported constants + added `type` assertion; custom `model.Extra` field handling preserved. Dropped all server.go/server_test.go hunks — they target home-mode code (`formatHomeClaudeModel`/`decodeHomeModels`/`homeModelEntry`/`handleHomeModels` + `strconv`/`registry` imports + `TestAmpProviderModelRoutes`/new home-model tests) removed on custom via removed-redis-home.md; conflicts in both files resolved by restoring HEAD; base commit advanced to a79ae80f) |
| 61 | b4bec344 | feat(translator): sanitize `parametersJsonSchema` in OpenAI to Gemini request handling | ❌ skipped (Gemini translator removed on custom branch: all 4 touched files live under `internal/translator/gemini/openai/{chat-completions,responses}/` and surface as modify/delete conflicts — `cherry-pick --no-commit` left them unmerged, then `git rm`'d; the shared util the commit *calls* (`util.CleanJSONSchemaForGemini` in `internal/util/gemini_schema.go`) is NOT modified by this commit and already exists intact on custom (`grep -n "func CleanJSONSchemaForGemini"` → line 27), so there is no surviving util hunk to integrate; identical to skip #54 bc652c7b; base commit NOT advanced) |
| 62 | 5771abbc | feat(management): add `ResetQuota` endpoint and auth manager quota reset functionality | ✅ picked (integrated `Manager.ResetQuota` in conductor.go + `Handler.ResetQuota` + `/reset-quota` route + quota_test.go/conductor_availability_test.go; server.go conflict resolved keeping only the new `reset-quota` line, dropping the `proxy-url`/`api-call`/`quota-exceeded/{switch-project,switch-preview-model}` routes removed on custom; recreated `quota.go` with ONLY the new `ResetQuota` handler since the dead `GetSwitchProject`/`GetSwitchPreviewModel` toggles (referencing the now-empty `QuotaExceeded` config struct) were removed on custom via 79ad5c30; added a local `authByIndex` helper to quota.go since custom removed `api_tools.go` where it lived upstream; fixed quota_test.go `config.Config{AuthDir:...}` → `config.Config{}` (AuthDir field removed via removed-auth-dir.md); conductor.go auto-merged clean; complementary to usage-limits/usage-persistence (different layer: runtime auth quota/cooldown state vs per-API-key token limits/SQLite store), no feature-doc updates needed; base commit advanced to 5771abbc) |
| 63 | 011ffe1d | feat(translator): enforce FIFO order in tool call ID consumption for Gemini requests | ❌ skipped (gemini translator code removed on custom branch) |
| 64 | 57e1bf97 | feat(translator): ensure preservation of tool and call IDs in Gemini request and response translations | ❌ skipped (gemini translator code removed on custom branch) |
| 65 | 09179a70 | feat(registry): add "max" level and remove deprecated Gemini models | ✅ picked (added "max" level to claude-sonnet-4-6; Gemini/Grok model hunks already removed on custom) |
| 66 | 35c3d80a | feat(translator): add support for handling video URLs in Gemini requests | ❌ skipped (Gemini translator code removed on custom branch) |
| 67 | eb8d0d06 | Merge pull request #3900 from sususu98/fix/antigravity-replay-fc-order-upstream-dev | ❌ skipped (antigravity code removed on custom branch; only surviving change is .gitignore entries already present from prior commits #26/#28) |
| 68 | bb414de3 | feat(api): add "max" reasoning depth and `service_tiers` to Codex client models | ✅ picked (clean cherry-pick; added "max" reasoning level + service_tiers to codex client models + test) |
| 69 | 9a8098d2 | feat(api): prioritize non-template Codex client models and adjust priority calculation logic | ✅ picked (clean cherry-pick; added non-template priority logic + test helper + priority assertion) |
| 70 | 1f21f946 | feat(api): implement support for multi-auth expansion in plugin systems | ❌ skipped (plugin system code removed on custom branch; all surviving file changes are plugin virtual auth / multi-auth expansion infrastructure that is inert when PluginAuthParser is nil) |
| 71 | 31549af1 | fix(watcher): update Gemini provider name to "gemini-cli" in file synthesizer logic | ✅ already-present (custom already skips Gemini files via immediate `return nil`; upstream renames to "gemini-cli" then skips — functionally identical) |
| 72 | 5bc0c682 | feat(pluginhost): improve error handling with HTTP status codes for plugin calls | ❌ skipped (plugin code removed on custom branch) |
| 73 | babef2a1 | feat(cliproxy): add `unregisterOpenAICompatExecutor` and sync runtime configuration | ⚠️ partial (integrated `unregisterOpenAICompatExecutor` method; dropped `hasNativeOpenAICompatExecutorConfig` (only caller is removed pluginHost block), pluginHost unregister call in `registerExecutorForAuth` (pluginHost removed), and `syncPluginRuntimeConfig`/`syncPluginModelRuntime` calls in `applyConfigUpdateWithAuthSynthesis` (both removed with plugin/home subsystem)) |
| 74 | 369e560f | feat(api): refactor provider key logic for API key usage and add test for compatibility grouping | ✅ integrated |
| 75 | 1f2504eb | fix(claude): bypass signature sanitizer for non-Claude models (#3946) | ✅ done |
| 76 | 079ec51f | feat(cliproxy): optimize API key alias rebuild with deferred execution and caching | ⚠️ partial (integrated `RefreshAPIKeyModelAlias`/deferred-rebuild context, `openAICompatibilityRegistrationCache`, dynamic worker limits, and `!forceReplace` executor cache check; dropped `registerModelRefreshCallback` + `refreshModelRegistrationForAuth*`/`latestAuthForModelRegistration` (depend on removed `registry.SetModelRefreshCallback`), `hasNativeOpenAICompatExecutorConfig` (only caller is removed pluginHost block), pluginHost check + `appendPluginModels` calls, and `syncPluginRuntime`→`reregisterExecutors` rename) |
| 77 | 36ed0e5c | fix(codex): strip model prefix for websocket payloads | ❌ skipped (codex executor code removed on custom branch) |
| 78 | 290f421f | Merge pull request #3959 from fdreamsu/codex/fix-codex-ws-prefix | ✅ done (merge: empty net diff on custom — codex subsystem removed; empty commit to advance base pointer) |
| 79 | c58da381 | feat(plugins): sync home plugin manifests | ⚠️ partial (integrated shared `EnsureIndex` whitespace-trim/normalize fix in `sdk/cliproxy/auth/types.go`; dropped all plugin/home subsystem files — `internal/homeplugins`, `internal/pluginhost`, `internal/pluginstore`, `sdk/pluginhost`, `sdk/pluginstore`, `sdk/cliproxy/home_plugins.go`, `internal/runtime/executor/helps/home_refresh_test.go` — and the home-mode wiring in `cmd/server/main.go` + `sdk/cliproxy/service.go` (`applyHomeOverlay`/`startHomeSubscriber`/`syncHomePlugins`), all removed on custom) |
| 80 | 5d9ea166 | Merge pull request #3963 from router-for-me/home | ✅ done (merge: empty net diff on custom — home/plugins subsystem removed and #79's `auth/types.go` fix already integrated; empty commit to advance base pointer) |
| 81 | bd646819 | test(translator, runtime): ensure empty text parts are skipped without null values | ❌ skipped (antigravity runtime executor + antigravity/gemini translators all removed on custom branch) |
| 82 | 7c390a7a | feat(runtime): add Claude Code session handling with caching and tests | ❌ skipped (modifies only removed codex/xai executors; new `claude_code_session.go` helper depends on removed CodexCache infra (`CodexCache`/`CodexPromptCacheKey`/`Get|SetCodexCacheRequired`, deleted in `00ccc5e2`) and its only callers were the removed executors; surviving Claude executor already has its own session handling via `CachedSessionIDRequired`) |
| 83 | f1ed8912 | feat(translator): wrap message-level system roles as user-visible reminders | ✅ done (integrated new `common/claude_system.go` + `ClaudeMessageSystemReminderText` and openai/claude request + test; dropped antigravity/gemini/codex hunks for removed translators) |
| 84 | 53a21dfb | [codex] Drop foreign encrypted_content before xAI Grok upstream (#3961) | ❌ skipped (xAI subsystem code removed on custom branch: 3 xAI executor files are modify/delete; new `grok_validation.go`+test are xAI support code whose only caller `InspectGrokEncryptedContent` was the removed xAI executor — zero callers on custom = dead code; `gemini_validation_test.go` additions `TestIsValidGeminiThoughtSignature_AgyNativeSamples`+helper depend on the absent Antigravity `agy-gemini-thought-signatures` corpus — always t.Skip on custom, tied to removed Antigravity subsystem; surviving `IsValidGeminiThoughtSignature` is NOT modified by this commit) |
| 85 | 05d1792d | feat(xai): replay Grok reasoning for Claude messages (#3962) | ❌ skipped (xAI subsystem code removed on custom branch: all 7 touched files absent — `internal/cache/` directory and every `xai_*` file were deleted; 4 modify/delete conflicts (xai_executor{,_test}.go, xai_websockets_executor.go, signature_cache.go) and 3 new files (xai_reasoning_replay_cache{,_test}.go, xai_reasoning_replay.go) whose only callers are the removed xAI executors — dead code on custom; no surviving shared change) |
| 86 | e9a11db7 | feat(home): enhance plugin management and synchronization | ❌ skipped (home/plugin/redis subsystem code removed on custom branch: 12 of 14 touched files absent (`home_plugin_status*.go`, `internal/config/home.go`, `internal/home/*`, `internal/homeplugins/*`, `internal/pluginhost/*`, `sdk/cliproxy/home_plugins*`); 2 shared files (`cmd/server/main.go`, `sdk/cliproxy/service.go`) have ONLY home/plugin/redis/storage wiring changes — `homeClient`/`home.New`/`homeplugins.SyncWithReport`/`reportHomePluginStatus`/`forceHomeRuntimeConfig`/`applyHomeOverlay`/`syncHomePlugins`/`processHomePluginTasks` + postgres/git/object store vars + `redisqueue`/`wsrelay`/`pluginhost` imports — all removed on custom via removed-redis-home.md/removed-storage-relay.md/removed-plugins.md; conflict "theirs" sides are entire removed subsystem blocks, no surviving shared change) |
| 87 | b89c594a | Merge pull request #3973 from router-for-me/home | ✅ done (merge: empty net diff on custom — merges #86 e9a11db7 home plugin management, all home/plugin/redis subsystem code removed on custom; all 9 modify/delete conflicts kept HEAD, 3 new home/plugin files removed as dead code, 2 shared files (main.go/service.go) resolved keeping HEAD; empty commit with original message+author to advance base pointer) |
| 88 | 70053bea | feat(auth): refactor credential kind detection and add dynamic source classification | ✅ done (integrated surviving shared refactor: new `sdk/cliproxy/auth/classification.go` (+test) with `AuthKind`/`AuthSourceKind` + attribute/source constants; `AccountInfo` reimplemented on `AuthKind`; `IsConfigAPIKeyAuth`/conductor/auto_refresh/usage_helpers/service.go switched to `AuthKind()`; surviving file/synthesizer/filestore write `AttributePath`/`AttributeSource`/`AttributeSourceBackend=AuthSourceFile`. Dropped hunks for removed subsystems: `internal/pluginhost/auth_provider.go` (plugins), `internal/store/{git,object,postgres}store.go` (storage backends), `sdk/cliproxy/auth/oauth_model_alias.go` `modelAliasChannel` (file removed), `service.go` `tryRegisterPluginModelsForAuth` block (plugins), `models.json` gemini-cli/kimi catalogs (removed providers), and filestore/file.go plugin-parsing blocks + `gemini-cli` condition (from other commits, not 70053bea). Resolved 5 modify/delete + 3 content conflicts. gofmt, go build, go test ./..., pytest 37/37, smoke test pass) |
| 89 | 3a13865d | refactor(home): relocate and rename home plugin status reporting logic | ❌ skipped (home code removed on custom branch: both renamed files target `internal/home/` which is removed (rename/delete), and the lone shared file `cmd/server/main.go` has a conflict whose "theirs" side is the entire home-JWT config-loading block + postgres/object/git store vars — all removed via removed-redis-home.md/removed-storage-relay.md; no surviving shared change) |
| 90 | 38ed7aef | fix(codex): sanitize downstream UA for direct image calls | ❌ skipped (codex code removed on custom branch: all 3 touched files `codex_executor.go`/`codex_openai_images.go`/`codex_openai_images_test.go` are modify/delete — the Codex executor and its image-call support were removed; no surviving shared change) |
| 91 | fd93ee03 | feat(oauth): add force-mapping for upstream response model rewrite | ❌ skipped (oauth code removed on custom branch: the `OAuthModelAlias`/`SanitizeOAuthModelAlias`/`Fork` symbols referenced by the `config.go`+`config.example.yaml` hunks and the `OAuthModelAliasResult`/`StreamRewriter` types referenced by the `conductor.go` hunks are all absent — the OAuth model-alias subsystem was removed via removed-providers-oauth.md; 3 modify/delete (`oauth_model_alias.go` x2, watcher/diff/oauth_model_alias.go) and 4 new files (`response_model_rewriter*.go`, `conductor_force_mapping_test.go`, `force_mapping_live_fixtures_test.go`) whose only callers are removed OAuth paths; config.example.yaml conflict is entirely the removed `vertex-api-key`/`oauth-model-alias`/`oauth-excluded-models` sections; no surviving shared change) |
| 92 | c20ac28a | Merge pull request #3981 from sususu98/feat/oauth-model-alias-force-mapping | ✅ done (merge: empty net diff on custom — merges #91 fd93ee03 oauth force-mapping, all OAuth model-alias subsystem code removed on custom; all modify/delete conflicts kept HEAD, 4 new oauth files removed as dead code, 3 shared-file content conflicts resolved keeping HEAD; empty commit with original message+author to advance base pointer) |
| 93 | 87e6d9cf | feat(videos): add model binding and propagation for video auth management | ❌ skipped (videos code removed on custom branch: both touched files `openai_videos_handlers.go`/`openai_videos_handlers_test.go` are modify/delete — the videos subsystem handlers were removed; no surviving shared change) |
| 94 | a183e729 | feat(auth): add ParseAuths method for expanding credential payloads into multiple auth records | ❌ skipped (pluginhost code removed on custom branch: the sole touched file `sdk/pluginhost/host.go` is modify/delete — the entire `pluginhost`/`pluginstore` plugin system was removed via removed-plugins.md; the new `ParseAuths` method only delegates to `h.inner.ParseAuths` on the plugin host, no surviving shared change) |
| 95 | a7250275 | fix(oauth): force-map responses to config alias not request suffix (#3983) | ❌ skipped (oauth code removed on custom branch) |
| 96 | df10a5b1 | feat(pluginhost): add shadow plugin management and cleanup functionality | ❌ skipped (pluginhost code removed on custom branch) |
| 97 | 7712ffed | Merge pull request #3986 from router-for-me/sdk | ✅ empty commit (merge of removed features) |
| 98 | b53d1e95 | refactor(pluginhost): replace `Snapshot().records` with `activeRecords` for improved filtering | ❌ skipped (pluginhost code removed on custom branch) |
| 99 | 810abe5e | feat(pluginhost): add OAuthProvider field to plugin metadata and update related functionality | ❌ skipped (pluginhost code removed on custom branch) |
| 100 | 29b53434 | Merge pull request #3998 from router-for-me/feat/plugin-OAuth | ✅ empty commit (merge of removed features) |
| 101 | 192888f9 | feat(pluginhost): enhance logging with plugin name and path fields | ⬜ pending |
| 102 | c4cf0fd3 | Merge pull request #4001 from router-for-me/plugin | ⬜ pending |
| 103 | eb2e1e33 | fix(auth): rewrite API key alias response models (#4002) | ⬜ pending |
| 104 | 7d1d2512 | docs: add Universal Chat Provider to "Who is with us?" | ⬜ pending |
| 105 | abe68cc1 | Merge pull request #4003 from maxdewald/add-universal-chat-provider | ⬜ pending |
| 106 | cb6992ef | docs: add Universal Chat Provider section to README files | ⬜ pending |
| 107 | 65f2288a | feat(models): refine Gemini 3.5 Flash variants and add Medium tier | ⬜ pending |
| 108 | 6a59d645 | feat(pluginhost): enhance plugin version management and logging for hot reload | ⬜ pending |
| 109 | 4c0c6029 | Merge pull request #4009 from router-for-me/plugin | ⬜ pending |
